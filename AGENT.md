# 奇怪的一角：项目上下文

## 项目定位

“奇怪的一角”是一个克制的个人作品站，用来收纳可打开的网页项目、轻量实验和少量值得保留的文字。站点不承担个人简历或社交主页的功能。

用户访问后应当快速理解三件事：这里在做什么、目前有哪些作品、作者如何处理个人信息。

## 设计方向

- 整体背景必须保持纯白，不使用暖灰、渐变或大面积主题色作为页面背景。
- 视觉气质参考 ChatGPT 网页版的安静、清晰和低干扰，但不直接复制布局或品牌元素。
- 主要通过留白、排版、细边框和内容层级建立质感，避免装饰性图片、强阴影和花哨动效。
- 页面文字统一使用黑白灰，不把绿色作为站点文字色。只有首页项目公告卡片可以使用各自项目的主题色。
- 首页与实验室的两个项目公告卡片保留同一套项目视觉：抽卡项目使用紫灰庄园风格，支线任务使用蓝绿粉的轻盈渐变。首页纵向排列，实验室可使用双栏展示。
- 桌面内容宽度以 `840px` 为主，站点导航最大宽度为 `1160px`。
- 所有核心页面都要支持手机端，不能出现横向滚动或被裁切的主要操作。
- 实验室宣传卡在 `760px` 以下必须保持卡内单列；不要让首页公告卡的 `30px + 1fr` 中间断点规则覆盖它。

设计令牌与组件样式集中在 `assets/css/style.css`，响应式覆盖集中在 `assets/css/responsive.css`。

## 内容与隐私边界

- 文案短、直接、诚实，不为了填满页面而制造内容。
- 不公开真实姓名、学校、专业、住址、私人联系方式或可推断日常轨迹的信息。
- 不写具体履历；关于页只说明创作方向和内容边界。
- 不加入访问统计、广告或追踪脚本。
- 不把 API Key、令牌、密码或其他秘密写入前端文件。
- 新增外部链接时使用 `target="_blank"`，并同时使用 `rel="noopener noreferrer"`。

## 技术架构

项目是无需构建步骤的静态站点，使用原生 HTML、CSS 和 JavaScript，可直接部署到 GitHub Pages。

```text
index.html                  首页
pages/projects.html         作品列表
pages/notes.html            文字页
pages/lab.html              实验与随机灵感
pages/about.html            站点介绍与隐私边界
assets/css/style.css        设计令牌、布局和组件
assets/css/responsive.css   手机与窄屏适配
assets/js/main.js           公共页面壳和页面交互
```

`assets/js/main.js` 中的 Web Components 统一生成导航和页脚：

- `<site-header active="home">` 用于首页。
- 子页面使用 `<site-header root="../" active="projects">` 这类写法。
- `<site-footer>` 在所有页面复用同一份页脚文案。
- 修改站名或导航时只修改 `main.js`，不要在每个 HTML 文件里复制导航。

作品内容目前保留在对应页面的 HTML 中，以维持静态页面的可读性和可索引性。只有真正跨多个页面、容易发生不一致的内容才值得进一步数据化。

## 文案口吻

- 优先使用短句和具体动词，例如“打开项目”“看看实验”。
- 避免夸张、自嘲、过度可爱或营销式表达。
- 可以保留一点个人语气，但不要通过个人经历来制造亲近感。
- 空页面允许诚实留白，不使用虚构文章或占位项目。

## 修改与验证流程

1. 修改前先确认 `git status`，保留用户已有的未提交改动。
2. 公共导航、页脚或站名优先修改 `assets/js/main.js`。
3. 颜色、间距、圆角和排版优先复用现有 CSS 变量与组件。
4. 修改 CSS 或 JS 后，同步更新 HTML 中的版本查询参数，避免部署缓存旧资源。
5. 在项目根目录运行 `python3 -m http.server 8000` 启动本地预览。
6. 交付前检查首页、作品、文字、实验和关于五个页面。
7. 至少验证一个约 `390px` 宽的手机视口，并确认实验页“换一句”按钮可用。
8. 检查浏览器控制台无错误，所有页面的当前导航状态正确。

## 部署前安全检查

每次部署都必须先执行以下检查；发现任何可疑信息时停止部署，先移除或确认：

1. 检查 `git status`、完整 diff 和待提交文件清单，只部署本次明确需要的源码与文档。
2. 扫描 API Key、访问令牌、密码、私钥、证书、`.env` 文件和疑似密钥赋值。
3. 扫描真实邮箱、手机号、地址、学校、专业、用户名以及 macOS、Linux 或 Windows 的本机绝对路径。
4. 确认 `work/`、设计复核截图、`design-qa.md`、系统文件和其他本地产物未进入暂存区。
5. 确认 Git 提交作者使用 GitHub `noreply` 邮箱，避免在提交元数据中暴露私人邮箱。
6. 对暂存内容再次运行安全扫描与 `git diff --cached --check`，通过后才允许推送或部署。
7. 部署完成后打开线上地址，确认首页与实验室能加载且发布内容与本地验证版本一致。

## 当前外部项目

- 奇怪的珍宝爽抽器：`https://identity-v-draw-simulator-cn.netlify.app/`
- 人生支线任务：`https://life-side-quest.netlify.app/`

如项目链接、名称或状态发生变化，应同步更新首页、作品页和实验页。
